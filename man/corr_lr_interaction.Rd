% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/06_diff_interaction.R
\name{corr_lr_interaction}
\alias{corr_lr_interaction}
\title{Correlation Analysis between Ligand-Receptor Interaction Scores and Continuous Metadata}
\usage{
corr_lr_interaction(lr_scores, metadata, correlate_with)
}
\arguments{
\item{lr_scores}{Data frame containing LR interaction scores per sample (data frame).}

\item{metadata}{Data frame containing sample metadata (data frame).}

\item{correlate_with}{Column name in \code{metadata} to compute correlation with (character).}
}
\value{
A data frame containing correlation results (correlation coefficient and p-values).
}
\description{
This function performs a Spearman correlation analysis between ligand-receptor (LR) interaction scores
(using raw scores) and a continuous metadata variable. It computes the correlation coefficient, p-value,
and adjusts p-values for multiple testing using the Benjamini-Hochberg method.
}
\examples{
data(lr_scores_eg)
data(metadata_eg)
res <- corr_lr_interaction(lr_scores_eg, metadata_eg, correlate_with = "Age_imputation")
head(res)
}
