% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/06_diff_interaction.R
\name{diff_lr_interaction}
\alias{diff_lr_interaction}
\title{Differential Ligand-Receptor Interaction Analysis between Two Groups}
\usage{
diff_lr_interaction(lr_scores, metadata, group_by, ident1, ident2)
}
\arguments{
\item{lr_scores}{Data frame containing LR interaction scores per sample (data frame).}

\item{metadata}{Data frame containing sample metadata (data frame).}

\item{group_by}{Column name in \code{metadata} to define sample groups (character).}

\item{ident1}{Value in \code{group_by} that defines the first group.}

\item{ident2}{Value in \code{group_by} that defines the second group.}
}
\value{
A data frame containing differential interaction results (mean scores, logFC, p-values).
}
\description{
This function performs a differential ligand-receptor (LR) interaction analysis between two sample groups.
It computes the average interaction scores per ligand-receptor-cell type (LRSR) pair for each group,
calculates the log fold change (logFC) between the groups, performs a Mann–Whitney U test (Wilcoxon rank-sum test)
for each LRSR pair, and adjusts the resulting p-values for multiple testing using the Benjamini-Hochberg method.
}
\examples{
data(lr_scores_eg)
data(metadata_eg)
res <- diff_lr_interaction(lr_scores_eg, metadata_eg, group_by = "Age_group",
  ident1 = "Young", ident2 = "Old")
head(res)
}
